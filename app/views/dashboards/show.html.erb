<%= form_for @dashboard.new_text_shout do |f| %>
	<%= f.text_field :body, placeholder: "Shout louder :O" %>
	<%= f.submit "Shout!" %>
<% end %>
<br/>

<%= form_for @dashboard.new_photo_shout do |f| %>
	<%= f.file_field :image%>
	<%= f.submit "Shout!" %>
<% end %>
<br/>


<%= render @dashboard.timeline %>